[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py310"
line-length = 100
exclude = ["checkpoints", "data"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
